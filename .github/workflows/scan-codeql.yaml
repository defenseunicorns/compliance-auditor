name: Scan CVEs

permissions:
  contents: read

on:
  push:
    branches: ["main"]
  pull_request:
    paths-ignore:
      - "**.md"
      - "**.jpg"
      - "**.png"
      - "**.gif"
      - "**.svg"
      - "adr/**"
      - "docs/**"
      - "CODEOWNERS"

  schedule:
    - cron: "32 2 * * 5"

jobs:
  scan-cves:
    name: Scan CVEs
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: ["go"]

    steps:
      - name: Checkout
        uses: actions/checkout@a5ac7e51b41094c92402da3b24376905380afc29 # v4.1.6

      - name: Setup golang
        uses: ./.github/actions/golang

      - name: Initialize CodeQL
        uses: github/codeql-action/init@9fdb3e49720b44c48891d036bb502feb25684276 # v3.25.6
        with:
          languages: ${{ matrix.language }}
          # config-file: ./.github/codeql.yaml #Uncomment once config file is needed.

      - name: Build
        run: make build

      - name: Perform CodeQL Analysis
        id: scan
        uses: github/codeql-action/analyze@9fdb3e49720b44c48891d036bb502feb25684276 # v3.25.6
        with:
          category: "/language:${{matrix.language}}"

      - name: Install CodeQL CLI
        run: |
          curl -LO https://github.com/github/codeql-cli-binaries/releases/download/d8845dbabdaf5c1ba56bfc223717efaf0c626e8b/codeql-linux64.zip
          unzip codeql-linux64.zip -d codeql
          sudo cp -r codeql/* /usr/local/bin/
          codeql --version

      - name: Analyze GitHub Actions Workflows
        run: |
          codeql database create --overwrite --language=yaml --source-root=.github/workflows codeql_actions_db
          codeql database analyze --format=sarif-latest --output=results/actions.sarif codeql_actions_db codeql/javascript-queries codeql/codeql-action

