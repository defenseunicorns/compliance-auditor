name: Scan CVEs

permissions:
  contents: read

on:
  push:
    branches: ["main"]
  pull_request:
    paths-ignore:
      - "**.md"
      - "**.jpg"
      - "**.png"
      - "**.gif"
      - "**.svg"
      - "adr/**"
      - "docs/**"
      - "CODEOWNERS"

  schedule:
    - cron: "32 2 * * 5"

jobs:
  scan-cves:
    name: Scan CVEs
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: ["go"]

    steps:
      - name: Checkout
        uses: actions/checkout@a5ac7e51b41094c92402da3b24376905380afc29 # v4.1.6

      - name: Setup golang
        uses: ./.github/actions/golang

      - name: Initialize CodeQL
        uses: github/codeql-action/init@9fdb3e49720b44c48891d036bb502feb25684276 # v3.25.6
        with:
          languages: ${{ matrix.language }}
          packs: codeql/codeql-action
          # config-file: ./.github/codeql.yaml #Uncomment once config file is needed.

      - name: Build
        run: make build

      - name: Perform CodeQL Analysis
        id: scan
        uses: github/codeql-action/analyze@9fdb3e49720b44c48891d036bb502feb25684276 # v3.25.6
        with:
          category: "/language:${{matrix.language}}"

      - name: Analyze GitHub Actions Workflows
        run: |
          codeql database create --overwrite --language=yaml --source-root=.github/workflows codeql_database
          codeql database analyze --format=sarif-latest --output=results/actions.sarif codeql_database codeql/javascript-queries codeql/codeql-action

